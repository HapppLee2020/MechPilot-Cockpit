# SW Agent Platform 架构规划文档

**SolidWorks 智能辅助平台 — 多工作站分布式任务编排系统**

| 版本 | 日期 | 部门 | 状态 |
|------|------|------|------|
| v1.0 | 2026-06-17 | 标准品硬件 | 规划评审中 |

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [系统总体架构](#2-系统总体架构)
3. [网络拓扑与部署方案](#3-网络拓扑与部署方案)
4. [Agent Server 核心模块设计](#4-agent-server-核心模块设计)
5. [任务生命周期管理](#5-任务生命周期管理)
6. [多工作站调度与负载均衡](#6-多工作站调度与负载均衡)
7. [规则知识库设计](#7-规则知识库设计)
8. [LLM Agent 编排策略](#8-llm-agent-编排策略)
9. [报告生成与消息推送](#9-报告生成与消息推送)
10. [SW Add-in 客户端设计](#10-sw-add-in-客户端设计)
11. [数据模型设计](#11-数据模型设计)
12. [安全性设计](#12-安全性设计)
13. [技术选型清单](#13-技术选型清单)
14. [实施路线图](#14-实施路线图)
15. [风险与应对](#15-风险与应对)

---

## 1. 项目背景与目标

### 1.1 现状痛点

- 硬件组 40-50 名工程师日常使用 SolidWorks + PDM，零件检入后需手动填写大量自定义属性（物料编码、名称、分类、材料、重量、表面处理等），重复劳动多，遗漏和填写不规范频发
- 属性检查、图纸审核等质量环节依赖人工逐项核对，效率低且标准执行不一致
- 已有的编码规则引擎（PDM MCP Server + SQL Server）能自动生成物料编码，但仅限于编码生成，未覆盖完整的属性填写流程
- 多台工作站（安装 SW + PDM + MCP Server）已有基础设施，但尚未形成统一的任务调度体系

### 1.2 项目目标

| 目标 | 说明 | 验收标准 |
|------|------|----------|
| 属性自动填写 | 工程师检入 PDM 后，一键触发 Agent 自动读取零件信息并按公司规则填写全部属性 | 常规零件属性填写准确率 ≥ 95%，单件处理时间 ≤ 30s |
| 属性合规检查 | 检查已填写属性的完整性和规范性，输出检查报告 | 必填字段遗漏检出率 100% |
| 多工作站并发 | 多台 SW 工作站可同时处理不同任务，Agent Server 统一调度 | 支持 ≥ 5 台工作站并发，单任务排队等待 ≤ 60s（非极端场景） |
| 多用户服务 | 支持 40-50 名工程师同时使用，统一排队和结果推送 | 系统支持 ≥ 40 并发用户，任务不丢失、不串号 |
| 规则可维护 | 公司标准和填写规则以自然语言维护，工程师可直接编辑，改完即时生效 | 规则变更零代码修改，修改后下一个任务立即生效 |
| 结果通知 | 任务完成后通过 Add-in 面板或飞书消息推送结果报告 | 双通道可达，从任务完成到工程师收到通知 ≤ 10s |

### 1.3 现有基础设施

| 组件 | 现状 | 本平台复用方式 |
|------|------|----------------|
| PDM Vault (SQL Server) | 10.254.62.8:50249, PdmCode 数据库, 52 条编码规则 | 直接复用，通过 MCP Worker 检入检出 |
| 编码规则引擎 | MCP 工具 pdm_generate_material_code，支持 auto_rename | 直接复用，Agent 调用 MCP 工具执行 |
| SW-Remote-MCP Server | 部署在多台工作站，提供 SW API 和 PDM 工具 | 作为 Worker Node，接受 Server 调度 |
| 飞书 CLI | 已部署 lark-cli v1.0.40，支持消息卡片推送 | 复用，作为消息推送通道 |

---

## 2. 系统总体架构

### 2.1 架构概览

![架构概览图](./SW_Agent_Platform_架构概览图.svg)

### 2.2 核心设计原则

| 原则 | 说明 |
|------|------|
| **工程师主动触发** | 任务由工程师通过 Add-in 按钮主动提交，不监听 PDM 检入事件。避免草稿/中间版本检入产生无效任务 |
| **Agent 编排，Worker 执行** | Agent（LLM）负责理解任务意图、生成执行计划；Worker 负责调用 SW/PDM API 完成实际操作。两者解耦 |
| **规则即文档，文档即规则** | 公司标准、填写规则以 Markdown 自然语言维护，工程师直接编辑，零代码修改 |
| **最小 LLM 调用** | 常规任务通过固定流程 + 规则引擎处理，仅在需要推理判断时调用 LLM。降低延迟和 API 成本 |
| **执行计划缓存** | LLM 生成的执行计划按文件特征缓存，相似零件直接复用历史计划，减少 LLM 调用 |
| **失败通知工程师** | PDM 文件锁冲突等不可恢复错误，重试后仍失败则通知工程师处理，不无限重试 |

### 2.3 组件职责划分

| 组件 | 职责 | 技术 |
|------|------|------|
| SW Add-in | 模型数据采集、任务提交、结果展示、进度轮询 | C# / .NET Framework |
| Agent Server | API 网关、任务队列、路由分发、规则加载、Worker 管理、报告生成、消息推送 | Python / FastAPI |
| MCP Worker | 执行 SW/PDM 操作、心跳上报、执行结果回传 | Python / SW-Remote-MCP (已有) |
| PDM Vault | 文件存储与版本管理、编码规则数据源 | SQL Server (已有) |
| LLM Cloud | 任务编排推理、报告内容生成 | 火山方舟 / Claude API |
| 飞书 | 任务完成通知推送 | lark-cli (已有) |

---

## 3. 网络拓扑与部署方案

### 3.1 网络拓扑

```
                           ┌─────────────┐
                           │   Internet   │
                           └──────┬──────┘
                                  │ WAN
                         ┌────────┴────────┐
                         │  Agent Server   │
                         │  (笔记本/服务器) │
                         │                │
                         │  网卡1: WAN    │  ← 连接外网，访问 LLM API
                         │  网卡2: LAN    │  ← 连接内网，接受 Add-in 请求
                         │  LAN IP: 固定   │
                         └────────┬────────┘
                                  │ LAN (HTTP)
                         ┌────────┴────────┐
                         │    LAN 交换机     │
                  ┌──────┼──────┬──────┬────┼──────┐
                  │      │      │      │    │      │
             Desktop1  Desktop2 Desktop3 ... DesktopN  Worker1~N
             (SW+Addin) (SW+Addin) ...      (SW+PDM+MCP)
```

### 3.2 部署约束

| 节点 | 网络环境 | 约束 | 部署要求 |
|------|----------|------|----------|
| 工程师 Desktop | LAN only，无外网 | 无法直接访问 LLM API | 安装 SW + PDM Client + Add-in DLL，配置 Server LAN IP |
| Agent Server | Dual NIC：WAN + LAN | LAN 侧固定 IP | 笔记本需有线接入 LAN，防止 Wi-Fi IP 变动导致客户端断连 |
| MCP Worker | LAN only | 需安装 SW + PDM，保持常驻运行 | IP 相对固定，SW 实例空闲时才能接受任务 |

> ⚠️ **关键风险**：如果 Agent Server 部署在笔记本上，笔记本休眠/关机 = 全组服务中断。建议评估是否部署到企业服务器，或至少确保笔记本长期在线且配置 Wake-on-LAN。

---

## 4. Agent Server 核心模块设计

### 4.1 模块架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Agent Server (FastAPI)                       │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  REST API Layer                                             │ │
│  │  POST /api/v1/task           提交任务                       │ │
│  │  GET  /api/v1/task/{id}      查询任务状态                   │ │
│  │  GET  /api/v1/task/{id}/result  获取任务结果                 │ │
│  │  GET  /api/v1/task/{id}/stream  SSE 流式进度                │ │
│  │  POST /api/v1/task/{id}/cancel  取消排队中任务              │ │
│  │  GET  /api/v1/workers        查看工作站状态                  │ │
│  │  GET  /api/v1/queue          查看队列状态                    │ │
│  │  POST /api/v1/rules/reload   手动触发规则重载                │ │
│  └────────────────────────┬────────────────────────────────────┘ │
│                           │                                      │
│  ┌────────────────────────▼────────────────────────────────────┐ │
│  │  Task Manager                                                │ │
│  │  - 创建任务 (生成 task_id, 写入 SQLite)                      │ │
│  │  - 状态流转 (queued → dispatched → executing → ...)         │ │
│  │  - 超时处理 (执行超时自动标记 failed)                         │ │
│  │  - 任务取消 (工程师可取消排队中任务)                          │ │
│  └────────────────────────┬────────────────────────────────────┘ │
│                           │                                      │
│  ┌────────────────────────▼────────────────────────────────────┐ │
│  │  Router (任务路由器)                                        │ │
│  │  逐个取任务，判断执行路线:                                   │ │
│  │                                                              │ │
│  │  ┌──────────────────┐  ┌───────────────────────────────┐    │ │
│  │  │ Fixed Pipeline   │  │ LLM Pipeline                  │    │ │
│  │  │ (规则可覆盖的任务) │  │ (需要推理判断的任务)           │    │ │
│  │  │                  │  │                               │    │ │
│  │  │ 属性填写(常规)   │  │ 属性填写(非标/新材料)          │    │ │
│  │  │ 属性检查         │  │ 图纸审核                      │    │ │
│  │  │ 编码生成         │  │ 需要人工确认的边界case         │    │ │
│  │  └────────┬─────────┘  └──────────┬────────────────────┘    │ │
│  │           │                       │                          │ │
│  │  ┌────────▼───────────────────────▼──────────────────────┐  │ │
│  │  │          Plan Cache (执行计划缓存)                      │  │ │
│  │  │          相似文件特征 → 历史执行计划 → 直接复用          │  │ │
│  └──────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────┬────────────────────────────────────┘ │
│                           │                                      │
│  ┌────────────────────────▼────────────────────────────────────┐ │
│  │  Worker Pool Manager                                         │ │
│  │  - 心跳检测 (每 3s 接收 Worker 上报)                         │ │
│  │  - 状态维护 (IDLE / BUSY / OFFLINE / ERROR)                 │ │
│  │  - 负载均衡 (Least Loaded + 文件亲和性)                      │ │
│  │  - 故障转移 (Worker 宕机 → 任务标记 failed + 通知)          │ │
│  │  - 锁预检 (PDM 文件是否被他人锁定)                           │ │
│  └────────────────────────┬────────────────────────────────────┘ │
│                           │                                      │
│  ┌────────────────────────▼────────────────────────────────────┐ │
│  │  Rule Store (规则知识库)                                     │ │
│  │  - 启动时全量加载 rules/*.md                                 │ │
│  │  - watchdog 监听文件变化，自动热重载                          │ │
│  │  - 按任务类型拼接相关规则注入 Agent prompt                    │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  Report Generator + Notifier                                 │ │
│  │  - 任务完成后调 LLM 生成人类可读报告                          │ │
│  │  - 写入 SQLite (Add-in 轮询获取)                              │ │
│  │  - 飞书卡片消息推送 (lark-cli)                                │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  Persistent Storage (SQLite)                                  │ │
│  │  tasks | workers | plan_cache | audit_log                    │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 API 设计

| 方法 | 路径 | 说明 | 请求体/参数 | 响应 |
|------|------|------|-------------|------|
| POST | `/api/v1/task` | 提交新任务 | `{task_type, filename, pdm_file_id, engineer, priority?}` | `{task_id, status, queue_position}` |
| GET | `/api/v1/task/{id}` | 查询任务状态 | — | `{status, progress, worker_id?, result?}` |
| GET | `/api/v1/task/{id}/stream` | SSE 流式进度 | — | SSE 事件流: `progress \| done \| failed` |
| GET | `/api/v1/task/{id}/result` | 获取任务结果/报告 | — | `{status, report, changes, audit_trail}` |
| POST | `/api/v1/task/{id}/cancel` | 取消排队中的任务 | — | `{success: true/false, reason?}` |
| GET | `/api/v1/workers` | 查看所有工作站状态 | — | `[{id, hostname, status, current_task?}]` |
| GET | `/api/v1/queue` | 查看当前队列 | — | `{total, by_priority: {P1: n, P2: n, ...}, estimated_wait}` |

---

## 5. 任务生命周期管理

### 5.1 状态机

```
                          ┌──────────────┐
                    ┌────►│   CANCELLED   │
                    │     └──────────────┘
                    │   (工程师取消)
                    │
┌──────────┐  dispatch  ┌───────────┐  assign  ┌───────────┐
│  QUEUED   ├──────────►│ DISPATCHED├────────►│ EXECUTING │
└──────────┘            └───────────┘          └─────┬─────┘
                                                      │
                                          ┌───────────┴───────────┐
                                          │                       │
                                          ▼                       ▼
                                   ┌────────────┐          ┌────────────┐
                                   │ REPORTING  │          │  FAILED    │
                                   │ (LLM生成报告)│          │ (含错误原因)│
                                   └──────┬─────┘          └──────┬─────┘
                                          │                       │
                                          ▼                       ▼
                                   ┌────────────┐          ┌────────────┐
                                   │  DELIVERING │          │  NOTIFIED  │
                                   │ (推送结果)   │          │ (通知工程师)│
                                   └──────┬─────┘          └────────────┘
                                          │
                                          ▼
                                   ┌────────────┐
                                   │    DONE    │
                                   └────────────┘
```

### 5.2 各状态说明

| 状态 | 触发条件 | 耗时预期 | Add-in 展示 |
|------|----------|----------|-------------|
| 🟡 QUEUED | 任务提交后进入队列，等待空闲 Worker | 取决于队列长度，通常 0-60s | "排队中 #3" |
| 🔵 DISPATCHED | 已分配 Worker，准备执行 | 1-2s（PDM 锁预检） | "分配到 WS-3" |
| 🔵 EXECUTING | Worker 正在调用 SW/PDM API | 5-30s（取决于任务复杂度） | "执行中 60%" |
| 🟢 REPORTING | 执行完成，LLM 生成报告 | 2-3s（LLM API 调用） | "生成报告中" |
| 🟢 DELIVERING | 推送报告到 Add-in 和飞书 | 1-2s | "通知已发送" |
| 🟢 DONE | 全流程完成 | — | "完成 ✓" + 展示报告 |
| 🔴 FAILED | 执行过程中发生不可恢复错误 | — | "失败 ✗: {错误原因}" |
| 🟡 CANCELLED | 工程师在 QUEUED 状态下主动取消 | — | "已取消" |

### 5.3 完整数据流示例（属性填写）

```
  Engineer Davis                           Agent Server                   Worker-3 (MCP)
       │                                       │                              │
  1. 点击 "属性填写"                            │                              │
       │                                       │                              │
  2. POST /task                               │                              │
  ────────►  { filename, pdm_file_id,          │                              │
               engineer: "davis",               │                              │
               task_type: "property_fill" }    │                              │
                                       3. 创建任务 #127，状态=QUEUED         │
                                       返回 { task_id: "127",               │
                                              position: 2 }                 │
       ◄────────────────────────────────────  │                              │
       │                                      │                              │
  4. 显示 "排队中 #2"                          │                              │
       │                                      │                              │
                                       5. Dispatcher 取出 #127               │
                                          Worker-3 IDLE，分配               │
                                          状态 → DISPATCHED                  │
                                       6. PDM 锁预检: pdm_get_file_info     │
                                           ────────────────────────────────► │
                                           ◄─── 文件未被锁定，可以检出 ──────  │
                                       7. 状态 → EXECUTING                  │
                                           (Router 判断: 常规零件，          │
                                            走 Fixed Pipeline 或             │
                                            LLM Pipeline)                    │
                                          │                                  │
                                       8. 如果走 Fixed Pipeline:            │
                                          读取规则 → 生成执行步骤            │
                                          如果走 LLM Pipeline:               │
                                          加载规则 → 调 LLM 编排             │
                                          → 生成执行计划 (JSON)              │
                                          │                                  │
                                       9. 按计划调用 MCP Worker:            │
                                          pdm_checkout_file(file_id)         │
                                           ────────────────────────────────► │
                                           ◄─────── 检出成功 ───────────────  │
                                          sw_read_all_properties()           │
                                           ────────────────────────────────► │
                                           ◄─────── {材质,重量,...} ────────  │
                                          sw_set_property("物料编码",...)     │
                                           ────────────────────────────────► │
                                           ◄─────── 写入成功 ───────────────  │
                                          sw_set_property("物料名称",...)     │
                                           ────────────────────────────────► │
                                           ◄─────── 写入成功 ───────────────  │
                                          ... (依次写入所有属性)              │
                                          pdm_checkin_file(file_id)           │
                                           ────────────────────────────────► │
                                           ◄─────── 检入成功 ───────────────  │
                                       10. 执行完成，收集改前/改后数据        │
                                       11. 状态 → REPORTING                  │
                                       12. 调 LLM 生成报告 (2-3s)            │
                                       13. 状态 → DELIVERING                  │
                                       14. 写入 SQLite                       │
                                       15. 飞书卡片推送                       │
                                       16. 状态 → DONE                       │
       │                                      │                              │
  17. Add-in 轮询/收到通知                    │                              │
       ◄────────────────────────────────────  │                              │
       │                                      │                              │
  18. 展示完成报告                             │                              │
       │ 同时飞书收到卡片消息                   │                              │
       │                                      │                              │
```

### 5.4 错误处理策略

| 错误场景 | 处理方式 | 通知 |
|----------|----------|------|
| PDM 文件被他人检出（锁冲突） | 指数退避重试 (5s → 15s → 45s)，最多 3 次 | 重试失败 → 通知工程师："文件被 [用户名] 检出中，请稍后再试" |
| Worker 宕机/离线 | 心跳超时 (10s) → 标记 Worker OFFLINE，任务标记 FAILED | 通知工程师："工作站异常，任务执行失败。已通知管理员" |
| SW 崩溃（Worker 侧） | Worker 本地自动重启 SW（需 Worker 端实现），超时后标记 FAILED | 通知工程师："SW 异常，任务执行失败" |
| LLM API 超时/限流 | 重试 1 次，仍失败则降级（跳过 LLM 编排，使用缓存计划或固定流程） | 通知工程师："部分属性需要人工确认" |
| 属性写入 PDM 后发现值错误 | LLM 报告中标注"需工程师确认"项，不做自动回滚 | 报告中用醒目标记，工程师在 PDM 中手动修正 |

---

## 6. 多工作站调度与负载均衡

### 6.1 Worker 状态模型

| 状态 | 含义 | 可接受新任务 | 触发转换 |
|------|------|-------------|----------|
| 🟢 IDLE | SW 空闲，可接受任务 | 是 | 任务完成 / 心跳恢复 |
| 🔵 BUSY | 正在执行任务 | 否 | 分配新任务 |
| 🔴 OFFLINE | MCP Server 不可达 | 否 | 心跳超时 (10s 无响应) |
| 🔴 ERROR | SW 崩溃或 PDM 连接断开 | 否 | Worker 上报错误状态 |
| 🟡 MAINTENANCE | 工程师手动标记维护中 | 否 | 通过 API 手动切换 |

### 6.2 心跳机制

```
Worker Node (每 3s):
  POST /api/v1/worker/heartbeat
  {
    "worker_id": "ws-3",
    "hostname": "HW-STATION-03",
    "status": "IDLE",          // 或 BUSY (附带当前 task_id)
    "sw_version": "2022",
    "pdm_connected": true
  }

Server:
  收到心跳 → 更新 workers 表的 last_heartbeat 时间戳
  后台线程每 10s 扫描:
    if last_heartbeat > 10s ago → 标记 OFFLINE
    if 当前有任务在此 Worker → 任务标记 FAILED + 通知工程师
```

### 6.3 调度策略

```
新任务进入队列
       │
       ▼
  筛选 IDLE Worker ──── 0 个 ──► 继续排队
       │
       ▼ (1 个或多个)
       │
  ┌────┴──────────────────────────────┐
  │  调度优先级 (按顺序判断):           │
  │                                    │
  │  1. 文件亲和性                     │
  │     同一装配体的零件优先分配给      │
  │     已处理过该装配体的 Worker      │
  │     (PDM 本地缓存命中率高)         │
  │                                    │
  │  2. PDM 锁预检                     │
  │     调 Worker 预检文件状态         │
  │     如果锁冲突 → 跳过该 Worker     │
  │                                    │
  │  3. Least Loaded                    │
  │     选累计完成任务数最少的          │
  │     (均匀分布负载)                 │
  │                                    │
  │  4. Round Robin (兜底)             │
  │     按顺序轮询                     │
  └────────────────────────────────────┘
```

> 💡 **关于文件亲和性**：同一装配体的多个零件如果分配给同一台 Worker 处理，该 Worker 的 PDM 本地缓存已经下载了相关文件，检出速度更快。可以在 Worker 上维护一个简单的"近期处理文件列表"，调度时查询是否有命中。

---

## 7. 规则知识库设计

### 7.1 目录结构

```
sw-agent/
├── rules/
│   ├── README.md                  # 编写指南（工程师从这里学习怎么写规则）
│   │
│   ├── general/                   # 通用规则 — 所有任务都加载
│   │   ├── 属性字段定义.md
│   │   ├── 命名规范.md
│   │   └── 编码规则说明.md
│   │
│   ├── materials/                 # 材料相关 — 属性填写时加载
│   │   ├── 常用材料清单.md
│   │   ├── 表面处理规则.md
│   │   └── 热处理要求.md
│   │
│   ├── categories/                # 零件分类 — 属性填写时加载
│   │   ├── 前缀分类映射.md
│   │   └── 非标件分类原则.md
│   │
│   ├── quality/                   # 质量检查 — 属性检查时加载
│   │   ├── 属性必填检查清单.md
│   │   └── 命名规范检查.md
│   │
│   └── drawing/                   # 图纸审核 — 图纸审核时加载
│       ├── 标题栏规范.md
│       └── 图纸格式要求.md
│
├── config/
│   ├── task_types.yaml            # 任务类型定义
│   ├── engineers.yaml             # 工程师信息映射
│   └── server.yaml                 # Server 配置
│
└── server/
    └── main.py                    # FastAPI Server 入口
```

### 7.2 任务类型与规则目录的映射关系

| 规则目录 | 适用的任务类型 | 说明 |
|----------|---------------|------|
| `general/` | 全部 (通配) | 所有任务都会加载的基础规则 |
| `materials/` | property_fill | 属性填写时需要材料/表面处理规则 |
| `categories/` | property_fill | 属性填写时需要分类规则 |
| `quality/` | property_check | 属性检查时需要检查清单 |
| `drawing/` | drawing_review | 图纸审核时需要审核规范 |

### 7.3 规则加载与注入流程

```
  任务进入 (task_type = "property_fill")
       │
       ▼
  Rule Store: 查映射表
       │
       ├── 加载 general/*.md        (所有任务通用)
       ├── 加载 materials/*.md      (属性填写专用)
       └── 加载 categories/*.md     (属性填写专用)
       │
       ▼
  拼接为完整规则文本 (~5000 tokens)
       │
       ▼
  注入到 Agent system prompt:
  ┌────────────────────────────────────────────────────────┐
  │  你是PDM属性填写助手。                                   │
  │                                                         │
  │  ## 当前任务                                             │
  │  类型: property_fill                                    │
  │  文件: bracket_left.SLDPRT                              │
  │                                                         │
  │  ## 公司规则 (必须严格遵守)                               │
  │  [这里插入拼接后的规则文本]                               │
  │                                                         │
  │  ## 可用工具                                             │
  │  [工具描述列表]                                          │
  │                                                         │
  │  ## 输出要求                                             │
  │  [格式要求]                                              │
  └────────────────────────────────────────────────────────┘
       │
       ▼
  Agent 在完整规则上下文中做决策
```

### 7.4 热加载机制

| 事件 | 行为 | 生效时机 |
|------|------|----------|
| 工程师编辑并保存 `.md` 文件 | Server 端 watchdog 检测到文件修改，重新读取该文件内容，更新内存缓存 | 下一个提交的任务立即使用新规则（无需重启 Server） |
| 新增 `.md` 文件到某个规则目录 | watchdog 检测到文件创建，加载到缓存 | 立即生效 |
| 删除 `.md` 文件 | watchdog 检测到文件删除，从缓存移除 | 立即生效 |
| 新增规则目录（如新增 `bom/`） | 需在配置中注册新目录的映射关系，然后重启 Server 或调 `/rules/reload` | 重启后生效 |

> ✅ **维护体验**：工程师的工作流：**打开 .md 文件 → 编辑 → 保存**。不需要登录任何平台，不需要懂 YAML/JSON 格式，不需要重启任何服务。下次提交任务时 Agent 就会使用新规则。修改历史通过 git 追溯，改错了可以 revert。

### 7.5 规则文件示例

#### 属性字段定义 (`general/属性字段定义.md`)

```markdown
# PDM 自定义属性字段

## 必填字段

以下字段在检入前必须填写，缺失时属性检查任务会报错：

| 字段名     | 说明     | 取值来源                 | 示例         |
|-----------|----------|-------------------------|-------------|
| 物料编码   | 9位编码  | 编码规则引擎自动生成     | 980012345   |
| 物料名称   | 中文名称 | 从文件名提取             | M8x30内六角螺柱 |
| 材料      | 材料名称 | 读取SW材质属性           | 304不锈钢   |
| 零部件分类  | 分类名称 | 按前缀映射规则判断       | 螺柱        |
| 重量(kg)   | 质量     | 读取SW质量属性           | 0.023       |
| 表面处理   | 处理方式 | 按材料+场景规则判断       | 发黑        |

## 选填字段

以下字段有值则填写，缺失不报错：

| 字段名   | 说明     | 取值来源                       |
|---------|----------|-------------------------------|
| 备注     | 特殊说明 | Agent根据零件特征判断是否需要标注 |
| 供应商   | 首选供应商| 标准件从标准件库查询，非标件留空  |
| 项目编号  | 所属项目 | 从PDM文件夹路径提取              |
```

#### 表面处理规则 (`materials/表面处理规则.md`)

```markdown
# 零件表面处理规则

## 按材料映射

| 材料              | 默认表面处理     | 备注                        |
|------------------|----------------|----------------------------|
| 碳钢(Q235/45钢)  | 发黑或镀锌      | 紧固件类默认发黑，结构件默认镀锌 |
| 不锈钢(304/316)  | 无或钝化         | 一般不处理，防腐要求高时做钝化   |
| 铝合金(6061/7075)| 阳极氧化         | 默认本色阳极氧化             |
| 铜合金(H62)      | 钝化或镀镍       | 导电件不做表面处理            |
| 尼龙(PA/PEEK)    | 无              | 塑料件不做表面处理            |

## 特殊情况

- 轴承安装孔相关零件：不处理表面
- 有配合要求的轴类零件：镀硬铬
- 食品/医疗相关零件：钝化处理，不允许镀铬
- 与电机轴接触的零件：不处理表面，避免镀层脱落
```

---

## 8. LLM Agent 编排策略

### 8.1 核心设计：编排与执行分离

```
  传统理解 (导致单任务瓶颈):
  ─────────────────────────────
  工程师 → LLM(理解+编排+等待执行+生成报告) → Worker → 返回 → LLM(继续) → ...
  整个过程 LLM 被一个任务占用，无法处理其他任务

本方案 (编排与执行分离):
  ─────────────────────────────
  
  阶段A: LLM 编排 (2-5s，生成执行计划)
  ──────────────────────────────
  输入: 任务上下文 + 规则文本 + 文件信息
  输出: JSON 执行计划 (工具调用列表)
  LLM 释放 → 可以处理下一个任务的编排

  阶段B: 异步执行 (5-30s，不占 LLM)
  ──────────────────────────────
  按计划顺序调用 MCP Worker
  完全 async，等待 Worker 时 CPU 释放
  多个任务的执行阶段可以完全并行

  阶段C: LLM 报告 (2-3s，生成人类可读报告)
  ──────────────────────────────
  输入: 执行结果 (改前/改后数据)
  输出: Markdown 报告文本
  LLM 释放

  流水线效果:
  ──────────────────────────────
  时间 →  0s      5s      10s     15s     20s     25s     30s

  Task-A: [编排A][    执行A(WS1)    ][报告A]
  Task-B:        [编排B][    执行B(WS2)    ][报告B]
  Task-C:              [编排C][    执行C(WS3)    ][报告C]
  Task-D:                    [编排D][    执行D(WS1)    ][报告D]

  LLM 在任何时刻只被一个阶段占用
  但不同任务的执行阶段完全并行
  实际并发 = Worker 数量 (5台 = 5个任务同时执行)
```

### 8.2 何时使用 LLM vs 固定流程

| 任务类型 | 场景 | 执行路线 | LLM 调用次数 | 说明 |
|----------|------|----------|-------------|------|
| 属性填写 | 标准件 / 文件名前缀可匹配 | Fixed Pipeline（缓存计划） | 0（首次 1 次，后续缓存） | 完全不需要 LLM，规则引擎直接计算 |
| 属性填写 | 常规零件（已知材料/分类） | Fixed Pipeline（缓存计划） | 0-1 | 类似零件已有缓存计划则直接复用 |
| 属性填写 | 非标零件 / 新材料 / 模糊场景 | LLM Pipeline | 1（编排）+ 1（报告） | LLM 生成执行计划，执行后生成报告 |
| 属性检查 | 全部 | Fixed Pipeline | 0（仅报告需 1 次） | 检查逻辑固定，报告可走 LLM 或模板 |
| 图纸审核 | 全部 | LLM Pipeline | 1（编排）+ 1（报告） | 需要 LLM 理解图纸内容 |

### 8.3 执行计划缓存

```
  缓存 Key: 文件特征指纹 (文件名前缀 + SW材质 + 零件类型)
  缓存 Value: JSON 执行计划

  第一次处理 "LSH-M8x30 螺柱.SLDPRT" (材质: 碳钢):
  ─────────────────────────────────────────────
  1. 无缓存 → LLM 编排 (3s)
  2. 生成计划 → 缓存: key="LSH_碳钢_螺柱", value=[步骤列表]
  3. 执行计划 → 完成

  第二次处理 "LSH-M10x40 螺柱.SLDPRT" (材质: 碳钢):
  ─────────────────────────────────────────────
  1. 匹配缓存 key="LSH_碳钢_螺柱" → 命中!
  2. 直接使用缓存计划执行 → 0s LLM
  3. 完成

  长期效果:
  ─────────────────────────────────────────────
  处理越多 → 缓存命中率越高 → LLM 调用越少
  常规零件 2 周后预计缓存命中率 > 80%
  LLM 主要处理新出现的非标零件
```

### 8.4 LLM 并发控制

> 💡 **当前阶段（单 Agent 进程）**
> FastAPI 使用 Python asyncio，单个进程中 LLM 调用是异步的。编排阶段（2-5s）和报告阶段（2-3s）分别调用 LLM，调用期间其他任务的执行阶段不受影响。LLM API 本身有限流（通常 5-10 并发），在当前规模下单进程完全够用。
>
> 如果未来 LLM 编排请求排队明显（同时有多个非标零件需要处理），可扩展为 Agent 实例池（多个 async 协程各自持有独立 LLM 会话），不改变整体架构。

---

## 9. 报告生成与消息推送

### 9.1 报告结构

| 字段 | 内容 | 来源 |
|------|------|------|
| 任务ID | #127 | 任务创建时生成 |
| 任务类型 | 属性填写 | 任务提交时指定 |
| 文件名 | bracket_left.SLDPRT | 任务提交时携带 |
| 工程师 | Davis | 任务提交时携带 |
| 执行工作站 | WS-3 | Worker 分配时记录 |
| 耗时 | 18s | 时间戳差值 |
| 变更摘要 | 属性变更对比表（改前/改后） | Worker 返回的执行结果 |
| Agent 备注 | 需要工程师确认的点 | LLM 生成 |
| 整体状态 | 已完成 / 部分完成 / 失败 | 综合判断 |
| 操作审计 | 执行步骤日志（工具调用记录） | Worker 返回 |

### 9.2 双通道推送

| 通道 | 机制 | 优势 | 适用场景 |
|------|------|------|----------|
| **Add-in 面板** | 轮询 `GET /task/{id}/result`，或在 SW 空闲时弹出通知 | 工程师在 SW 内直接查看，不打断工作流 | 所有任务（必选） |
| **飞书消息** | 任务完成后通过 lark-cli 推送卡片消息 | 工程师不在 SW 前时也能收到通知 | 工程师可配置是否启用 |

### 9.3 飞书卡片消息格式

```
┌──────────────────────────────────────────────┐
│  ✅ 任务完成 · 属性填写                         │
│──────────────────────────────────────────────│
│  文件: bracket_left.SLDPRT                    │
│  工程师: Davis                                 │
│  耗时: 18s · 工作站: WS-3                     │
│──────────────────────────────────────────────│
│  📋 变更摘要                                  │
│  ┌──────────┬──────────────────┐               │
│  │ 物料编码  │ 980012345        │               │
│  │ 物料名称  │ 左支架            │               │
│  │ 材料     │ AL6061-T6        │               │
│  │ 分类     │ 支架类            │               │
│  │ 重量     │ 0.085 kg         │               │
│  │ 表面处理  │ 阳极氧化          │               │
│  └──────────┴──────────────────┘               │
│──────────────────────────────────────────────│
│  💬 Agent: 材质为铝合金，建议阳极氧化处理。     │
│  如有特殊要求请工程师确认。                      │
│──────────────────────────────────────────────│
│  ✅ 已检入 PDM                                │
│                                               │
│  [查看详情] [反馈问题]                          │
└──────────────────────────────────────────────┘
```

### 9.4 工程师身份映射

```yaml
# config/engineers.yaml
engineers:
  davis:
    name: "Davis"
    feishu_id: "ou_c678a9b3dd9965f3b02188f331517565"
    notify: ["addin", "feishu"]    # 双通道通知

  zhangsan:
    name: "张三"
    feishu_id: "ou_xxxxxxxxx"
    notify: ["feishu"]             # 仅飞书

  lisi:
    name: "李四"
    feishu_id: ""
    notify: ["addin"]              # 仅 Add-in
```

---

## 10. SW Add-in 客户端设计

### 10.1 职责边界

> ⚠️ **核心原则：Add-in 只做三件事**
> 1. **模型数据采集** — 通过 SW API 获取当前打开模型的元数据（文件名、路径、PDM 状态等）
> 2. **任务提交与状态查询** — 调用 Agent Server REST API，提交任务并轮询状态
> 3. **结果展示** — 在 SwTaskPane（嵌入 HTML 页面）中渲染报告
>
> Add-in 不做：规则判断、LLM 调用、PDM 操作、任务调度。所有这些都在 Server 端完成。

### 10.2 UI 布局

```
┌────────────────────────────────────────────────────────┐
│  SW CommandManager — 自定义 Tab: "Agent助手"            │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 属性填写  │ │ 属性检查  │ │ 图纸审核  │ │ 通用问答  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  SwTaskPane — 任务面板 (HTML)                            │
│                                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  当前文件: bracket_left.SLDPRT                   │   │
│  │  PDM状态: 已检入 ✓                               │   │
│  │  [属性填写] [属性检查] [图纸审核]                   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ── 执行状态 ──                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  #127 属性填写  ● 执行中 (WS-3)                   │   │
│  │  检出 → 读取 → 填写 → 检入   [========] 60%        │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ── 最近任务 ──                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  #126 属性检查   ✓ 完成   12:30   3s              │   │
│  │  #125 属性填写   ✓ 完成   12:15   18s             │   │
│  │  #124 图纸审核   ✓ 完成   11:50   45s             │   │
│  │  #123 属性填写   ✗ 失败   11:30   "文件被锁定"     │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  [设置] — Server地址 / 通知偏好 / 工程师身份              │
└────────────────────────────────────────────────────────┘
```

### 10.3 配置文件

```json
{
  "server_url": "http://192.168.1.100:8080",
  "poll_interval_ms": 3000,
  "timeout_seconds": 120,
  "engineer_id": "davis"
}
```

---

## 11. 数据模型设计

### 11.1 SQLite 表结构

#### tasks 表 — 任务队列

```sql
CREATE TABLE tasks (
    id              TEXT PRIMARY KEY,     -- task_id (UUID)
    task_type       TEXT NOT NULL,        -- property_fill / property_check / drawing_review
    status          TEXT NOT NULL,        -- queued / dispatched / executing / 
                                          -- reporting / delivering / done / failed / cancelled
    
    priority        INTEGER DEFAULT 3,    -- 1(最高) ~ 5(最低)
    filename        TEXT NOT NULL,
    pdm_file_id     TEXT,
    engineer_id     TEXT NOT NULL,
    
    payload         TEXT,                 -- JSON: 任务提交时的原始数据
    execution_plan  TEXT,                 -- JSON: LLM 生成的执行计划（可缓存）
    execution_log   TEXT,                 -- JSON: Worker 执行步骤日志
    changes         TEXT,                 -- JSON: 属性改前/改后对比
    report          TEXT,                 -- Markdown: LLM 生成的报告
    error           TEXT,                 -- 错误信息（失败时）
    
    assigned_worker TEXT,                 -- worker_id
    created_at      TEXT NOT NULL,
    dispatched_at   TEXT,
    started_at      TEXT,
    finished_at     TEXT,
    reported_at     TEXT
);

CREATE INDEX idx_tasks_status ON tasks(status);
CREATE INDEX idx_tasks_engineer ON tasks(engineer_id);
CREATE INDEX idx_tasks_priority_created ON tasks(priority, created_at);
```

#### workers 表 — 工作站状态

```sql
CREATE TABLE workers (
    id              TEXT PRIMARY KEY,     -- worker_id (如 "ws-1", "ws-2")
    hostname        TEXT,
    status          TEXT NOT NULL,        -- idle / busy / offline / error / maintenance
    current_task_id TEXT,                 -- 正在执行的任务ID
    sw_version      TEXT,
    pdm_connected   INTEGER DEFAULT 1,    -- 0/1
    
    recent_files    TEXT,                 -- JSON: 近期处理过的文件列表（用于亲和性调度）
    
    last_heartbeat  TEXT,
    created_at      TEXT NOT NULL
);
```

#### plan_cache 表 — 执行计划缓存

```sql
CREATE TABLE plan_cache (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    feature_key     TEXT NOT NULL,        -- 文件特征指纹（前缀+材质+类型）
    plan            TEXT NOT NULL,        -- JSON: 执行计划
    hit_count       INTEGER DEFAULT 0,    -- 命中次数
    last_used_at    TEXT,
    created_at      TEXT NOT NULL,
    
    UNIQUE(feature_key)
);
```

#### audit_log 表 — 操作审计

```sql
CREATE TABLE audit_log (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id         TEXT,
    worker_id       TEXT,
    engineer_id     TEXT,
    action          TEXT NOT NULL,        -- task_created / dispatched / step_executed / 
                                          -- plan_generated / report_sent / failed / cancelled
    detail          TEXT,                 -- JSON: 操作详情
    created_at      TEXT NOT NULL
);

CREATE INDEX idx_audit_task ON audit_log(task_id);
CREATE INDEX idx_audit_time ON audit_log(created_at);
```

---

## 12. 安全性设计

| 维度 | 风险 | 措施 |
|------|------|------|
| **API Key 安全** | LLM API Key 泄露 | Key 仅存储在 Agent Server 端，不传递到任何客户端或 Worker。Add-in 和 Worker 均无法接触 API Key |
| **内网通信** | Add-in 与 Server 之间的请求被篡改 | 初期使用 HTTP（内网隔离）。如安全合规要求，可升级为 HTTPS + API Token 认证 |
| **操作审计** | Agent 误操作修改属性无法追溯 | 全部操作写入 audit_log，记录改前/改后的值、操作时间、执行 Worker、触发工程师 |
| **权限控制** | 非授权人员提交任务 | 通过 engineer_id 做基础身份校验（Add-in 配置中绑定 Windows 登录名）。可扩展为 API Token |
| **PDM 数据安全** | Agent 错误检入导致数据覆盖 | Agent 写入属性后工程师可在 PDM 中查看变更。如发现错误，可通过 PDM 版本历史回退 |
| **规则篡改** | 非授权人员修改规则文件 | 规则目录权限控制 + git 操作日志追溯。可扩展为规则变更需审批 |

---

## 13. 技术选型清单

| 组件 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| Agent Server | Python + FastAPI + Uvicorn | Python 3.12+ | 团队已有 Python 生态基础（Rhea/MCP），FastAPI 原生异步、自动 API 文档 |
| 任务队列存储 | SQLite | 3.x | 轻量、无部署依赖、单机够用（已有编码规则引擎 SQLite 经验） |
| LLM SDK | Anthropic SDK / OpenAI SDK | 最新 | 兼容火山方舟（OpenAI 兼容接口）和 Claude API |
| 文件监听 | watchdog | 最新 | Python 标准 FS 监听库，跨平台 |
| SW Add-in | C# + SwVisualStudioTemplates | .NET Framework 4.8 | SW 2022 Add-in 标准开发方式 |
| Add-in UI | SwTaskPane (嵌入 HTML) | — | 轻量 UI，快速渲染报告内容，无需 WPF 复杂布局 |
| 飞书推送 | lark-cli | 1.0.40 | 已有基础设施，直接复用 |
| MCP Worker | SW-Remote-MCP Server | 已有 | 已有基础设施，直接复用 |
| 版本管理 | git | — | 管理规则文件和配置变更 |

---

## 14. 实施路线图

| 阶段 | 内容 | 交付物 | 预估时间 | 验收标准 |
|------|------|--------|----------|----------|
| 🟢 P0 | FastAPI Server 骨架 + SQLite 任务表 + 手动 curl 端到端验证 | 可运行的 Server + 任务提交/查询 API | 1-2 天 | curl 提交任务 → SQLite 记录 → 查询返回 |
| 🟢 P1 | Router + 单 Worker 对接（打通 MCP 调用链路） | Server → MCP Worker → SW/PDM 操作 → 结果返回 | 2-3 天 | 提交任务 → Server 调 Worker → 自动填写一个零件的属性 |
| 🔵 P2 | Rule Store + 规则加载 + LLM 编排集成 | 规则热加载 + Agent 按规则填写属性 | 3-5 天 | 编辑 .md 规则 → 保存 → 下一个任务使用新规则 |
| 🔵 P3 | 多 Worker 调度 + 心跳 + 负载均衡 | Worker Pool Manager + 心跳机制 | 2-3 天 | 2 台 Worker 同时处理不同任务，互不干扰 |
| 🟡 P4 | 报告生成 + Add-in 面板 + 飞书推送 | Add-in DLL + TaskPane UI + 飞书卡片 | 5-7 天 | 端到端：Add-in 提交 → 处理 → 报告展示 + 飞书通知 |
| 🟡 P5 | 执行计划缓存 + 错误处理完善 + 审计日志 | 缓存机制 + 重试策略 + 审计表 | 2-3 天 | 重复零件第二次处理不调 LLM；错误场景有明确通知 |
| 🔴 P6 | Add-in 分发 + 工程师配置 + 上线部署 | 网络共享部署 + 配置指南 + 规则编写指南 | 3-5 天 | 3-5 名工程师试用 1 周，收集反馈迭代 |

> 💡 **预估总工期**：P0-P3（Server 核心）约 2 周，P4-P6（客户端 + 上线）约 2 周，总计 3-4 周。P4 的 Add-in 开发工作量最大，如果团队有 C# SW Add-in 开发经验可以缩短。

---

## 15. 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| Agent Server 单点故障（笔记本方案） | 中 | 高 — 全组无法使用 | 优先评估企业服务器部署方案；笔记本方案需确保有线接入 + 防休眠 + UPS。Server 启动脚本做到 1 分钟可恢复 |
| PDM 文件锁冲突频繁 | 中 | 中 — 部分任务延迟 | 重试 + 通知工程师；长期方案：与团队约定 "Agent 处理时段"，减少人工操作冲突 |
| Worker 侧 SW 崩溃 | 中 | 中 — 单任务失败 | Worker 本地守护进程自动重启 SW；Server 超时检测 + 任务重新入队 |
| LLM API 不稳定 / 限流 | 低 | 中 — LLM 任务延迟 | 执行计划缓存降级（使用历史计划）；重试机制；可选多 LLM Provider 备用切换 |
| 规则维护质量下降（工程师胡乱编辑） | 中 | 中 — 属性填写不准确 | git 版本管理 + 变更通知（飞书推送规则变更记录）；定期审查规则质量 |
| Add-in 与不同版本 SW 的兼容性 | 低 | 高 — 部分工程师无法使用 | 统一 SW 版本（当前 2022）；Add-in 兼容性测试 |
| 40+ 工程师并发提交导致队列积压 | 低 | 低 — 排队等待 | 队列位置透明展示（Add-in 显示排队位置和预估等待时间）；优先级机制保证紧急任务先处理 |
